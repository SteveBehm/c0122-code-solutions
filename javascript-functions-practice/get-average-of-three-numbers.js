/* exported getAverageOfThreeNumbers */
// function getAverageOfThreeNumbers(x, y, z) {
//   var avgOfThree = (x + y + z) / 3;
//   return avgOfThree;
// }

let getAverageOfThreeNumbers = (x, y, z) => (x + y + z) / 3;
