/* exported getAverageOfTwoNumbers */
// function getAverageOfTwoNumbers(x, y) {
//   var averageOfTwo = (x + y) / 2;
//   return averageOfTwo;
// }

let getAverageOfTwoNumbers = (x, y) => (x + y) / 2;
