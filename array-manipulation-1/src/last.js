/* exported last */
/*
look at the group given
go to the last member of the group
it will be the last when there is no member after the current member
look at the value of that member
if there is a value there
return that value
*/
function last(array) {
  return array[array.length - 1];
}
